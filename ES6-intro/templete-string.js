function numbers(num1, num2) {
  const result = `total no is ${num1 + num2}`;
  //   console.log(result);
}
numbers(100, 300);

function discount(price) {
  const output = `this product ${price} discount 20% is ${price * 0.8}`;
  console.log(output);
}
discount(100);
